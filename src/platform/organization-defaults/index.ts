/**
 * Epic OM-9.5 — Organisation Default Seeding & UX Stabilisation.
 *
 * Public surface. Consumers (UI, health checks, release readiness) should
 * import from '@/platform/organization-defaults' only.
 */
export * from './defaultsEvents';
export * from './defaultsHealth';
